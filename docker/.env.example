# Docker production env - copy to .env and customize
#   cp .env.example .env
#
# Used by docker-compose.prod.yml for Backend + Redis.
# Authentication is handled by Supabase (hosted) — no auth server needed in Docker.

# No additional Docker-specific variables required.
# Backend env vars (SUPABASE_*, MONGODB_URI, etc.) are configured in ../.env.production
