name: Deploy Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Deploy containers
        run: |
          cd C:\Users\balwa\Documents\bill-pro-max-backend-v2
          git pull origin main
          cd docker
          docker compose -f docker-compose.prod.yml up -d --build